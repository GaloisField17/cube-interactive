export const DEFAULT_COLORS = Object.freeze({
  right: "#0d0",
  left: "#07f",
  top: "yellow",
  bottom: "#eee",
  front: "red",
  back: "orange",
  inner: "#222",
});

export const DEFAULT_SIZE = 1;
export const DEFAULT_GAP = 0.035;
